/*
 * GraphLine Messages
 *
 * This contains all the text for the GraphLine component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.GraphLine';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GraphLine component!',
  },
});
